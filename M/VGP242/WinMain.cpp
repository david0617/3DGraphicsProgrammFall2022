#include <Windows.h>
#include "../Framework/Core/Inc/Core.h"

int WinMain(HINSTANCE, HINSTANCE, LPSTR, int)
{
    while (true)
    {
        LOG("New Log");
        Sleep(0);
    }

    // game init
    // game run
    // game cleanup
    return 0;
}